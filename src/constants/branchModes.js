const branchModes = {
  standart: "standart",
  select: "select",
  transform: "transform",
  
  forwardAxisX: "forwardAxisX",
  forwardAxisY: "forwardAxisY",
  forwardAxisZ: "forwardAxisZ",
  backAxisX: "backAxisX",
  backAxisY: "backAxisY",
  backAxisZ: "backAxisZ",
};

export default branchModes;
